define({
  // Number of toppings to place in a given workstation's platter.
  freeToppingCount: 5,
  // Duration (in milliseconds) to enforce when navigating between
  // workstations.
  workstationTransitionDelay: 1000
});
